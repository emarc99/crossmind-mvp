fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0
httpx>=0.25.0
openai>=1.3.0
requests>=2.31.0

# Fetch.ai uAgents framework for autonomous agents
# Note: uagents requires Python <3.13, will install via separate step

# Web3 and blockchain utilities
web3>=6.11.0
eth-account>=0.10.0

# Data processing
pandas>=2.1.0
numpy>=1.24.0

# Async utilities
aiohttp>=3.9.0

# Logging and monitoring
python-json-logger>=2.0.0

# Note:
# - Avail Nexus Core will be installed from Avail's npm registry via frontend
# - uAgents requires Python 3.9-3.12 (will need to downgrade Python for backend agent development)
# - MeTTa will be installed via separate command with appropriate Python version
